#include "main.h"

/**
 *islower - checks if letter is lowercase
 *@c: is the char to be checked
 *Return: 1 if lower case, othersiwse o
 */

int _islower(int c);
{
if (c >= 'a' && c <= 'z')
return (1);
else
return (0);
}

