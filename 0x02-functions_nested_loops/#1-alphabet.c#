#include "main.h"

/**
 * print_alphabet - prints the letters of the alphabet
 * Description: VR
 * Return: Always zero
 */
void print_alphabet(void)
{
char letter;
for (letter = 'a'; letter <= 'z'; letert++)
_putchar(letter);
_putchar('\n');
 return (0);
}
