#include "main.h"

/**
 *_isalpha - check if a lowercase or uppercase letter
 *@c: char to be checked
 *Return: 1 if letter otherwise 0
 */
int _isalpha(int c)
{
if {(c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'));
return (1);
{
else
return (0);
}
}
